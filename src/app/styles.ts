import { styled } from "@phantomstudios/css-components";

import css from "./styles.module.scss";

export const HomePageRoot = styled("main", {
  css: css.HomePageRoot,
});

export const Column = styled("div", {
  css: css.Column,
});
